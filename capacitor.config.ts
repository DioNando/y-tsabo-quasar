import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ytsabo.app',
  appName: 'y-tsabo-quasar',
  webDir: 'dist/spa',
  bundledWebRuntime: false
};

export default config;
